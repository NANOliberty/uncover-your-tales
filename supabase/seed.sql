-- =====================================================================
-- 로컬 개발용 seed.
-- `supabase db reset` 시 자동 적용된다.
-- 운영(원격)에는 절대 들어가지 않는다.
-- =====================================================================

-- M1.2 에서 인증/그룹이 실제로 동작하기 시작하면
-- 여기에 더미 그룹 / 더미 캐릭터 / 더미 시나리오를 채운다.
--
-- 지금은 비워둔다 — 스키마만 검증하면 충분.
